extract embedded HTML from RTF documents
========================================

HTML email sent by MS Outlook is embedded within (TNEF-encoded) RTF. This library helps extract the HTML.

This package is split-off from an earlier "pytnef" package.
